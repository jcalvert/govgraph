<%= form_for(@metropoli) do |f| %>
  <% if @metropoli.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metropoli.errors.count, "error") %> prohibited this metropoli from being saved:</h2>

      <ul>
      <% @metropoli.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :population %><br />
    <%= f.text_field :population %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
